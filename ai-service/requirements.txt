flask==3.1.0
flask-cors==5.0.1
flask-limiter==3.12
# Redis client — required by flask-limiter when REDIS_URL is set (storage backend).
# Without this, the ai-service container fails to boot in Docker where
# REDIS_URL=redis://redis:6379 is set via docker-compose.yml.
redis>=5.0,<6.0
psycopg2-binary==2.9.10
python-dotenv==1.1.0
gunicorn==23.0.0
gevent==24.11.1
openai==1.82.0
scikit-learn>=1.3.0
pytest-cov>=4.0
