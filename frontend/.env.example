# Frontend environment variables (Vite)
# Copy this file to .env.local and adjust values as needed.

# Backend API base URL
# - Leave empty for development (Vite proxy forwards requests to localhost:8080)
# - Set to the full backend origin for production builds
VITE_API_URL=
